using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GunElementsGenerator : MonoBehaviour
{
    // Are needed for generating exactly 1 part of a gun
    public bool StopGeneratingGunFlare = false;
    public bool StopGeneratingGunFrame = false;
    public bool StopGeneratingGunTrigger = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
